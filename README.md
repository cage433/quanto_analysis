Analysis of quanto options

run the scripts quanto_option_'n'_analysis to see
values for random generated options and market data.

The outputs show the analytic, monte carlo and dynamically hedged values
